<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JavaApp"
        tools:targetApi="31">
        <activity
            android:name=".ViewInvoices"
            android:windowSoftInputMode="adjustPan"
            android:exported="false" />
        <activity
            android:name=".CreateInvoice"
            android:windowSoftInputMode="adjustPan"
            android:exported="false" />
        <activity
            android:name=".AddClass"
            android:windowSoftInputMode="adjustPan"
            android:exported="false" />
        <activity
            android:name=".SignUpView"
            android:windowSoftInputMode="adjustPan"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".AddClient$InvoiceView" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name=".DanceClassView"
            android:windowSoftInputMode="adjustPan"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".database.DanceClassView" -->
        <!-- android:exported="false" -->
        <!-- android:label="@string/title_activity_dance_class_view" -->
        <!-- android:theme="@style/Theme.JavaApp" /> -->
        <activity
            android:name=".ClassSignUp"
            android:windowSoftInputMode="adjustPan"
            android:exported="false" />
        <activity
            android:name=".ClientView"
            android:windowSoftInputMode="adjustPan"
            android:exported="false" />
        <activity
            android:name=".AddClient"
            android:windowSoftInputMode="adjustPan"
            android:exported="false" />
        <activity
            android:name=".HomePage"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>